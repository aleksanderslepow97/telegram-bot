# Telegram Bot для оплаты услуг

Бот для приема платежей и продажи услуг в Telegram.

## Функциональность

- 💳 Пополнение баланса через различные платежные системы
- 🛒 Покупка услуг с внутреннего баланса
- 👥 Управление пользователями и платежами (для администраторов)
- 📊 Статистика и аналитика
- 💼 Управление услугами (создание, редактирование, удаление)

## Требования

- Python 3.8+
- PostgreSQL/SQLite (по умолчанию используется SQLite)
- Redis (опционально, для хранения состояний)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd payment-telegram-bot
Установите зависимости:

bash
pip install -r requirements.txt
Создайте файл .env на основе .env.example:

bash
cp .env.example .env
Настройте переменные окружения в файле .env:

env
# Токен бота от @BotFather
BOT_TOKEN=your_bot_token_here

# Токен для Telegram Payments (получить у @BotFather)
PAYMENT_PROVIDER_TOKEN=your_payment_provider_token_here

# Настройки ЮKassa (опционально)
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key

# ID администраторов (через запятую)
ADMINS=123456789,987654321

# URL базы данных (по умолчанию SQLite)
DATABASE_URL=sqlite:///payments.db

# URL Redis (опционально)
REDIS_URL=redis://localhost:6379/0
Настройка платежных систем
Telegram Payments
Создайте бота через @BotFather

Включите платежи в @BotFather: /mybots -> Ваш бот -> Bot Settings -> Payments

Добавьте платежного провайдера (например, ЮMoney или другого)

Получите PAYMENT_PROVIDER_TOKEN

ЮKassa
Зарегистрируйтесь на kassa.yandex.ru

Создайте магазин и получите SHOP_ID и SECRET_KEY

Настройте вебхуки для уведомлений о платежах

Запуск бота
bash
python bot.py
Использование
Для пользователей
Начните диалог с ботом: /start

Пополните баланс через кнопку "💳 Пополнить баланс"

Выберите услугу в разделе "🛒 Услуги"

Оплатите услугу с внутреннего баланса

Для администраторов
Добавьте свой Telegram ID в переменную ADMINS в .env

Используйте команду /admin для доступа к админ-панели

Управляйте услугами, пользователями и платежами

Структура проекта
text
payment-telegram-bot/
├── bot.py              # Основной код бота
├── config.py           # Конфигурация
├── database.py         # Модели базы данных
├── keyboards.py        # Клавиатуры
├── payment_system.py   # Платежные системы
├── requirements.txt    # Зависимости
├── .env               # Переменные окружения
└── README.md          # Документация
Расширение функциональности
Добавление новой платежной системы
Создайте класс, наследующий от PaymentSystem

Реализуйте методы create_payment и check_payment

Добавьте инициализацию в PaymentManager

Добавление новых услуг
Используйте админ-панель или

Добавьте услуги напрямую в базу данных

Безопасность
Храните секретные ключи в .env файле

Не коммитьте .env в репозиторий

Используйте HTTPS для вебхуков

Регулярно обновляйте зависимости

Лицензия
MIT

text

## Шаг 8: Запуск и активация бота

### 1. Создание бота в Telegram

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Введите имя вашего бота (например, "Payment Service Bot")
4. Введите username бота (должен заканчиваться на `bot`, например, `payment_service_bot`)
5. Сохраните полученный токен

### 2. Настройка платежей в BotFather

1. Вернитесь к @BotFather
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите `Bot Settings`
5. Выберите `Payments`
6. Добавьте платежного провайдера (например, ЮMoney или другой)
7. Получите `PAYMENT_PROVIDER_TOKEN`

### 3. Настройка окружения

1. Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=ваш_токен_от_botfather
PAYMENT_PROVIDER_TOKEN=ваш_платежный_токен
ADMINS=ваш_telegram_id
4. Установка зависимостей
bash
pip install -r requirements.txt
5. Запуск бота
bash
python bot.py
6. Первоначальная настройка
Откройте Telegram и найдите вашего бота по username

Отправьте команду /start

Если ваш ID указан в ADMINS, используйте /admin для управления ботом

Дополнительные настройки
Настройка вебхуков (для продакшена)
Для продакшена рекомендуется использовать вебхуки вместо polling:

python
# Вместо executor.start_polling используйте:
WEBHOOK_HOST = 'ваш_домен'
WEBHOOK_PATH = '/path/to/webhook'
WEBHOOK_URL = f"{WEBHOOK_HOST}{WEBHOOK_PATH}"

executor.start_webhook(
    dispatcher=dp,
    webhook_path=WEBHOOK_PATH,
    on_startup=on_startup,
    on_shutdown=on_shutdown,
    skip_updates=True,
    host='0.0.0.0',
    port=8443,
)
Настройка SSL для вебхуков
Для вебхуков требуется SSL сертификат. Вы можете использовать Let's Encrypt или самоподписанный сертификат.

Тестирование платежей
Для тестирования платежей через Telegram Payments:

Используйте тестовые карты:

1111 1111 1111 1026 (успешный платеж)

1111 1111 1111 1025 (неуспешный платеж)

Или используйте тестовый токен от BotFather:

Тестовый провайдер: TEST

Тестовый токен: TEST:test